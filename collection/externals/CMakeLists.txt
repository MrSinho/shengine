cmake_minimum_required(VERSION 3.0)
add_definitions(-DCMAKE_EXPORT_COMPILE_COMMANDS=ON)


if (NOT TARGET shvulkan)
add_subdirectory(shvulkan)
endif()
#if (NOT TARGET shecs)
#add_subdirectory(shecs)
#endif()
if (NOT TARGET plyimporter)
add_subdirectory(plyimporter)
endif()
if (NOT TARGET shserial)
add_subdirectory(shserial)
endif()
#if (NOT TARGET shthreads)
#add_subdirectory(shthreads)
#endif()
if (NOT TARGET shgui)
add_subdirectory(shgui)
endif()

if (NOT TARGET glfw)
add_subdirectory(glfw)
endif()
if (NOT TARGET cglm)
add_subdirectory(cglm)
endif()
if (NOT TARGET smd)
add_subdirectory(smd)
endif()

